/**
 * Sandbox stub for wire-elements/pro — see
 * ../../src/Components/Spotlight/SpotlightServiceProvider.php for context.
 *
 * resources/js/app.js imports the real package's spotlight-component.js
 * purely for its side effects (it isn't imported for any exported value),
 * to register the Spotlight Alpine/Livewire component client-side. This
 * empty stub lets `vite build` resolve that import and produce a real
 * manifest for tests that render pages, without needing the actual
 * (licensed, unavailable here) component — the Spotlight UI just won't be
 * interactive in this sandbox, which no test here exercises.
 */
